[package]
name = "codex-spec-workflow-desktop"
version = "0.1.0"
description = "Desktop wrapper for the Codex Spec Workflow dashboard"
edition = "2021"

[lib]
name = "codex_spec_workflow_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
